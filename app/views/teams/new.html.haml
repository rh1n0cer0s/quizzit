%p.lead Create your team:

- if flash[:error]
  %p.alert.alert-error= flash[:error]

= labelled_form_for(@team) do |f|
  = f.text_field :name
  %p Invite Team Members:
  = f.fields_for :members do |mf|
    = mf.text_field :email
  = f.submit_button "Create!"

